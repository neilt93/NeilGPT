# Core ML dependencies
torch>=2.0.0
numpy>=1.24.0

# Data processing
pandas>=2.0.0
pyyaml>=6.0

# Training utilities
tqdm>=4.65.0
tensorboard>=2.13.0

# Tokenization
sentencepiece>=0.1.99

# Content filtering (MANDATORY - PII removal)
spacy>=3.7.0

# Development
jupyter>=1.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Deployment (adjust based on chosen option)
# Option 1: FastAPI backend
fastapi>=0.104.0
uvic orn>=0.24.0
pydantic>=2.0.0

# Option 2: Gradio (if needed for quick demo)
# gradio>=4.0.0

# After installing, download spaCy model:
# python -m spacy download en_core_web_sm
